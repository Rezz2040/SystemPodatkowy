<Window x:Class="SystemPodatkowy.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:lvc="clr-namespace:LiveCharts.Wpf;assembly=LiveCharts.Wpf"
        Title="System Rozliczania Podatków" Height="700" Width="1000" WindowStartupLocation="CenterScreen">

    <Window.Resources>
        <Style TargetType="Button" x:Key="ModernButton">
            <Setter Property="Background" Value="#0078D7"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Padding" Value="20,10"/>
            <Setter Property="MinHeight" Value="36"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Margin" Value="5"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="5">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" Margin="{TemplateBinding Padding}"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#005A9E"/>
                </Trigger>

                <Trigger Property="IsEnabled" Value="False">
                    <Setter Property="Background" Value="#E9ECEF"/>
                    <Setter Property="Foreground" Value="#ADB5BD"/>
                    <Setter Property="Cursor" Value="Arrow"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style TargetType="Button" x:Key="SuccessButton" BasedOn="{StaticResource ModernButton}">
            <Setter Property="Background" Value="#28A745"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#218838"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style TargetType="Button" x:Key="NeutralButton" BasedOn="{StaticResource ModernButton}">
            <Setter Property="Background" Value="#6C757D"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#5A6268"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style TargetType="Button" x:Key="RowActionButton" BasedOn="{StaticResource ModernButton}">
            <Setter Property="Background" Value="#DC3545"/>
            <Setter Property="Content" Value="Usuń"/>
            <Setter Property="Padding" Value="15,5"/>
            <Setter Property="MinHeight" Value="28"/>
            <Setter Property="Margin" Value="5,2"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#C82333"/>
                </Trigger>

                <DataTrigger Binding="{Binding IsDeleted}" Value="True">
                    <Setter Property="Background" Value="#28A745"/>
                    <Setter Property="Content" Value="Przywróć"/>
                </DataTrigger>

                <MultiDataTrigger>
                    <MultiDataTrigger.Conditions>
                        <Condition Binding="{Binding IsDeleted}" Value="True"/>
                        <Condition Binding="{Binding Path=IsMouseOver, RelativeSource={RelativeSource Self}}" Value="True"/>
                    </MultiDataTrigger.Conditions>
                    <Setter Property="Background" Value="#218838"/>
                </MultiDataTrigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="5"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Background="#F8F9FA" BorderBrush="#E9ECEF" BorderThickness="0,0,0,1" Margin="0,0,0,15" Padding="10,5">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0" Orientation="Horizontal" Margin="0,0,20,0">
                    <Button Content="Odśwież Listę" Command="{Binding LoadTaxpayersCommand}" Style="{StaticResource NeutralButton}"/>
                    <CheckBox Content="Pokaż usuniętych podatników" IsChecked="{Binding ShowDeletedTaxpayers}" VerticalAlignment="Center" Margin="10,0,0,0"/>
                </StackPanel>

                <StackPanel Grid.Column="1" Orientation="Horizontal">
                    <Button Content="+ Dodaj Podatnika" Command="{Binding AddTaxpayerCommand}" Style="{StaticResource ModernButton}"/>
                    <Button Content="+ Dodaj Powierzchnię" Command="{Binding AddPropertyAreaCommand}" Style="{StaticResource ModernButton}"/>
                    <Button Content="Zaksięguj Wpłatę" Command="{Binding AddPaymentCommand}" Style="{StaticResource ModernButton}"/>
                    <Button Content="Generuj Deklarację" Command="{Binding GenerateDeclarationCommand}" Style="{StaticResource ModernButton}"/>
                </StackPanel>

                <StackPanel Grid.Column="3" Orientation="Horizontal">
                    <Button Content="Zarządzaj Stawkami" Command="{Binding OpenTaxRatesCommand}" Style="{StaticResource NeutralButton}"/>
                    <Button Content="Zapisz Edycję" Command="{Binding SaveInlineEditCommand}" Style="{StaticResource SuccessButton}" Margin="15,5,5,5"/>
                </StackPanel>
            </Grid>
        </Border>

        <DataGrid Grid.Row="1" ItemsSource="{Binding Taxpayers}" SelectedItem="{Binding SelectedTaxpayer}" AutoGenerateColumns="False" SelectionMode="Single" CanUserAddRows="False">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding TaxpayerID}" Width="50" IsReadOnly="True"/>
                <DataGridTextColumn Header="Typ" Binding="{Binding EntityType}" Width="120"/>
                <DataGridTextColumn Header="Nazwa/Nazwisko" Binding="{Binding CompanyNameOrLastName}" Width="*"/>
                <DataGridTextColumn Header="Imię" Binding="{Binding FirstName}" Width="120"/>
                <DataGridTextColumn Header="NIP" Binding="{Binding TaxIdNumber}" Width="100"/>
                <DataGridTextColumn Header="PESEL" Binding="{Binding PersonalIdNumber}" Width="110"/>
                <DataGridTextColumn Header="Miejscowość" Binding="{Binding City}" Width="150"/>

                <DataGridTemplateColumn Width="110">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Button Style="{StaticResource RowActionButton}"
                    Command="{Binding DataContext.DeleteTaxpayerCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}"
                    CommandParameter="{Binding}"/>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>

            </DataGrid.Columns>
        </DataGrid>

        <GridSplitter Grid.Row="2" Height="5" HorizontalAlignment="Stretch" Background="#DDDDDD" Margin="0,2,0,2"/>

        <TabControl Grid.Row="3" Margin="0,5,0,0">

            <TabItem Header="Powierzchnie">
                <DataGrid ItemsSource="{Binding SelectedTaxpayer.PropertyAreas}" AutoGenerateColumns="False" IsReadOnly="True">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Obręb" Binding="{Binding CadastralDistrict}" Width="150"/>
                        <DataGridTextColumn Header="Nr Działki" Binding="{Binding PlotNumber}" Width="100"/>
                        <DataGridTextColumn Header="Powierzchnia (m²)" Binding="{Binding TotalAreaSqm}" Width="120"/>
                        <DataGridTextColumn Header="Adres" Binding="{Binding Address}" Width="*"/>
                    </DataGrid.Columns>
                </DataGrid>
            </TabItem>
            
            <TabItem Header="Historia Wpłat">
                <DataGrid ItemsSource="{Binding SelectedTaxpayer.Payments}" AutoGenerateColumns="False" IsReadOnly="True">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Data Wpływu" Binding="{Binding PaymentDate, StringFormat=d}" Width="120"/>
                        <DataGridTextColumn Header="Sposób Wpłaty" Binding="{Binding PaymentMethod}" Width="150"/>
                        <DataGridTextColumn Header="Kwota" Binding="{Binding Amount, StringFormat=C}" Width="120"/>
                    </DataGrid.Columns>
                </DataGrid>
            </TabItem>

            <TabItem Header="Deklaracje">
                <DataGrid ItemsSource="{Binding SelectedTaxpayer.TaxDeclarations}" AutoGenerateColumns="False" IsReadOnly="True" RowDetailsVisibilityMode="VisibleWhenSelected">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Rok" Binding="{Binding Year}" Width="80"/>
                        <DataGridTextColumn Header="Data Złożenia" Binding="{Binding SubmissionDate, StringFormat=d}" Width="120"/>
                        <DataGridTextColumn Header="Typ Dokumentu" Binding="{Binding DocumentType}" Width="120"/>
                        <DataGridTextColumn Header="Łączny Podatek" Binding="{Binding TotalTaxAmount, StringFormat=C}" Width="150"/>
                    </DataGrid.Columns>

                    <DataGrid.RowDetailsTemplate>
                        <DataTemplate>
                            <Border Background="#FAFAFA" BorderBrush="#EEEEEE" BorderThickness="1" Margin="20,5,5,15" Padding="10">
                                <StackPanel>
                                    <TextBlock Text="Pozycje tej deklaracji:" FontWeight="Bold" Margin="0,0,0,5" Foreground="#555"/>
                                    <DataGrid ItemsSource="{Binding DeclarationItems}" AutoGenerateColumns="False" IsReadOnly="True" HeadersVisibility="Column" Background="White">
                                        <DataGrid.Columns>
                                            <DataGridTextColumn Header="Pow. Opodatkowana (m²)" Binding="{Binding TaxableAreaSqm}" Width="180"/>
                                            <DataGridTextColumn Header="Wartość (PLN)" Binding="{Binding ItemValue, StringFormat=C}" Width="150"/>
                                        </DataGrid.Columns>
                                    </DataGrid>
                                </StackPanel>
                            </Border>
                        </DataTemplate>
                    </DataGrid.RowDetailsTemplate>
                </DataGrid>
            </TabItem>

            <TabItem Header="Analityka Wpłat">
                <Grid Background="White" Margin="15">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="200"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <TextBlock Grid.Column="0" Text="Kluczowi płatnicy miasta" FontWeight="Bold" FontSize="16" VerticalAlignment="Center" TextWrapping="Wrap"/>

                    <lvc:PieChart Grid.Column="1" 
                              Series="{Binding PaymentPieChartData}" 
                              LegendLocation="Right" 
                              InnerRadius="70" 
                              Margin="20">
                    </lvc:PieChart>
                </Grid>
            </TabItem>

        </TabControl>
    </Grid>
</Window>