<Window x:Class="SystemPodatkowy.GenerujDeklaracjeWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Generuj Deklarację" Height="200" Width="300" WindowStartupLocation="CenterScreen">
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <TextBlock Grid.Row="0" Grid.ColumnSpan="2" Text="Generowanie deklaracji podatkowej" FontWeight="Bold" Margin="0,0,0,15" HorizontalAlignment="Center"/>

        <TextBlock Grid.Row="1" Grid.Column="0" Text="Rok podatkowy:" Margin="0,0,10,0" VerticalAlignment="Center"/>
        <TextBox Grid.Row="1" Grid.Column="1" Text="{Binding Rok, UpdateSourceTrigger=PropertyChanged}" Width="80" HorizontalAlignment="Left" VerticalAlignment="Center"/>

        <Button Grid.Row="2" Grid.ColumnSpan="2" Content="Generuj" Command="{Binding GenerujCommand}" Width="100" Height="30" HorizontalAlignment="Center" VerticalAlignment="Bottom" Margin="0,15,0,0"/>
    </Grid>
</Window>